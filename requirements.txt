# Brain
groq>=0.11.0                 # Groq SDK — the DEFAULT brain (llm.provider: groq)
google-genai>=1.0.0          # Gemini SDK — only if llm.provider: gemini

# Voice (install the one(s) you use)
chatterbox-tts              # OPTIONAL, GPU only: Resemble AI Chatterbox (Turbo + Multilingual), MIT, CUDA.
                            # Skip on Mac/CPU — pulls torch. Only needed for tts.provider: chatterbox*
edge-tts>=7.0.0             # FREE Microsoft neural TTS (English + Arabic) — no API key, no GPU
imageio-ffmpeg              # ffmpeg binary edge-tts uses to decode mp3 -> PCM16
cartesia>=2.0.0              # Cartesia Sonic streaming TTS (optional)
elevenlabs>=1.0.0            # ElevenLabs Flash streaming TTS (optional, paid)

# Audio out -> virtual device (BlackHole on Mac, PulseAudio null sink on Linux)
sounddevice>=0.4.6
soundfile                   # read/write voice-clone reference wavs
numpy

# Stream + OBS
TikTokLive>=6.6.5
obsws-python>=1.7.0

# Misc
pyyaml
python-dotenv
