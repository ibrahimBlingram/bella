# Brain
google-genai>=1.0.0          # Gemini SDK (from google import genai)

# Voice (install the one(s) you use)
edge-tts>=7.0.0             # FREE Microsoft neural TTS (Arabic) — no API key
cartesia>=2.0.0              # Cartesia Sonic streaming TTS (optional)
elevenlabs>=1.0.0            # ElevenLabs Flash streaming TTS (optional, paid)

# Audio out -> virtual device (BlackHole on Mac)
sounddevice>=0.4.6
numpy

# Stream + OBS
TikTokLive>=6.6.5
obsws-python>=1.7.0

# Misc
pyyaml
python-dotenv
