# Brain
google-genai>=1.0.0          # Gemini SDK (from google import genai)

# Voice (install the one(s) you use)
cartesia>=2.0.0              # Cartesia Sonic streaming TTS
elevenlabs>=1.0.0            # ElevenLabs Flash streaming TTS

# Audio out -> virtual device (BlackHole on Mac)
sounddevice>=0.4.6
numpy

# Stream + OBS
TikTokLive>=6.6.5
obsws-python>=1.7.0

# Misc
pyyaml
python-dotenv
